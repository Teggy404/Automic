export type Make = {
  name: string;
};

export type Makes = Make[];

export type Model = {
  name: string;
};

export type Models = Model[];

export type Year = {
  id: string;
  stringYear: string;
};

export type Years = Year[];
